# Properly-cited draft

The +6.4pp headline on chart/abc123xy holds up under strict-mature scoping.
The 7% lift in [[2026-05-20-pin-cohort-scope-before-iterating-reads]] confirms
the direction. State.yaml field (state.yaml.experiments.checkout_redesign_v2.latest_read.summary)
records the 1.5x ratio as well.

Every numerical value above has a citation hint within 100 chars; the
citation-traceability check should fire zero findings.
