# A draft with neither pointers nor numbers

This file contains prose only. It mentions no Amplitude charts, no state
files, no vault findings, and no numerical claims that need citation.

It exists to validate the "empty result" edge case for both pointer
extraction and the citation-traceability check.
