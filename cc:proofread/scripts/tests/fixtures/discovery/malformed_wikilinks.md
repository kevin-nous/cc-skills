# Malformed-wikilink edge fixture

This draft mentions [[a-valid-link]] alongside an unterminated [[unclosed
form and a bare [bracket]. The valid one should extract; the malformed
ones should be ignored without crashing.

It also mentions chart/abc123xy inside a single line so we have a
non-zero number of pointers to confirm extraction still completes.
