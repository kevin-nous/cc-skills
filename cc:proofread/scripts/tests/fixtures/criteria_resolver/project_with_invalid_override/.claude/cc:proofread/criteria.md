# Invalid project criteria

This file has no YAML frontmatter and therefore no schema_version. The
resolver must raise InvalidProjectCriteriaError rather than silently fall
back to the shipped default — the "fail loudly" rule is load-bearing.

## Scope auditor

## Grounding auditor

## Structural auditor

## Mechanical prepass
