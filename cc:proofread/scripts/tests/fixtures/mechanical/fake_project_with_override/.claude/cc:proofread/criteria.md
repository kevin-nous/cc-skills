---
schema_version: 1
description: Per-project override for ccp-017 throttle tests
mechanical_prepass:
  max_findings_per_check:
    default: 3
    hedge-promotion-regex: 7
---

## Scope auditor

(suppressed — test fixture only carries the cap config)

## Grounding auditor

(suppressed — test fixture only carries the cap config)

## Structural auditor

(suppressed — test fixture only carries the cap config)

## Mechanical prepass

(suppressed — test fixture only carries the cap config)
